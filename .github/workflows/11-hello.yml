name: Hello

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: print hello
        run: | 
          echo "Hello World"
      - name: print date
        run: echo "The date is $(date)"
      - name: print dir contents
        run: ls -l